@model IEnumerable<kiemtra.Models.sanpham>

@{
    ViewBag.Title = "Index";
    Layout = null;
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.dmsanpham.tenDM)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.tenSP)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.xuatSU)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.namSX)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.dgia)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.dmsanpham.tenDM)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tenSP)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.xuatSU)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.namSX)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.dgia)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.maSP }) |
            @Html.ActionLink("Details", "Details", new { id=item.maSP }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.maSP })
        </td>
    </tr>
}

</table>
